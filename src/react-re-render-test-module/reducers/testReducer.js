/**
 *  Created by cl on 2018/6/8
 */

export default (state = {status: 0}, action) => {
    switch(action.type){
        case 're-render-module-test':
            return state;
        default:
            return state;
    }
}